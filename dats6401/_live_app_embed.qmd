::: {.callout-tip title="Live app"}
The course demo app, running. Three tabs, one idea each: mapping variables to
visual channels (Week 2), ordering a correlation matrix so its blocks become
visible (Week 3), and what a truncated axis does to a trend (Week 4).

```{=html}
<iframe
  src="https://dataviz-qev9d3yyjfbjztzfzsobnj.streamlit.app/?embed=true"
  width="100%" height="600" style="border:1px solid #ddd; border-radius:8px;"
  loading="lazy">
</iframe>
```

Source: [`demo_app/`](https://github.com/gaimsds/dataviz/tree/main/demo_app). The
`?embed=true` on the URL is what hides Streamlit's own chrome inside the frame.
Community Cloud apps sleep when idle, so the first load after a quiet spell takes
a few seconds to wake.
:::
